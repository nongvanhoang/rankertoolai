# Scout Report — 2026-08-18

## Operational note

`build_dashboard.py` no longer exists on `main` (it was intentionally `git rm --cached` + gitignored on 2026-07-18, commit `36fe6bf2`, after a live-exposure incident where internal scripts and per-tool commission data were being served publicly at `/dashboard/`). It also does not exist as `html/build_dashboard.py` — `main` has no `html/` subdirectory; the repo root itself is the deploy root.

This scan instead used, as the "already covered" source:
1. `build_dashboard.py`'s `AFFILIATE_DB` dict, which is still tracked (with its own independent commit history) on the `scout-log` branch itself — 32 entries.
2. Unioned with the actual `review/`, `alternatives/`, `compare/`, and `go/` directory listings read from `main` — 164 slugs, a more complete picture of live coverage than `AFFILIATE_DB` alone (e.g. Perplexity, Grok, Mistral, Windsurf, Tabnine, Codeium have live review pages but no `AFFILIATE_DB` entry).

Nothing new was found or reported as a result of this substitution — it only made the "already covered" set larger/safer, never smaller. Worth fixing at the source, though: consider committing a small non-sensitive `covered_slugs.json` (slugs only, no commission data) to `main` for the scout to read directly, since `main` no longer carries anything the scout can use for this step.

## New / Upgraded tools

None today. Pulse scan covered Product Hunt AI top launches, AI startup funding (24–48h), affiliate-network new listings (PartnerStack/Impact/Rewardful), Reddit/X AI-builder chatter, and Futurepedia/TAAFT trending — see candidates evaluated below.

Candidates surfaced and evaluated, all scored PASS (composite < 5, driven by red flags) and dropped per Step 4:

- **Moltcraft** — open-source pixel-world dashboard for Moltbot agents. No pricing page (cloud tier "coming soon," waitlist only); free/open-source today. Narrow niche (single agent framework). Red flag: no real pricing page.
- **Clears** (clears.ai) — agentic SDLC execution platform, ranked well on Product Hunt. No public self-serve pricing found (sales-led, "connect in 30 minutes"). Red flag: no real pricing page.
- **Pickle Browser** (picklebrowser.com) — local browser-automation tool for AI agents. Explicitly free, $0/run, no account. Red flag: no monetizable pricing.
- **AirJelly** (airjelly.ai) — proactive desktop "second brain" agent, macOS only. Free download, no published paid tier yet. Red flag: no real pricing page.

Also checked and passed over:
- **Ellis AI** — $10M seed, AI agents for private credit managers. Extremely narrow enterprise vertical, low market_size fit for a consumer/SMB affiliate site.
- **June** — $20M pre-seed (Benioff's Time Ventures), product/category not yet public. Too early to score; will re-check on next scan once it ships something concrete.
- **Grok Bot** — already has a live `/go/grok/` redirect page on the site; not new.

## Repeats (suppressed from detail, log updated)

6 tools already in `scout_log.json` reappeared in today's Product Hunt top-launches results, confirming continued traction — `last_seen` and `tier_history` updated, same tier as last known, no report detail per Step 5:

- Wispr Flow (HOT, 8.0)
- Hey Noah (WATCH, 6.75)
- AdAnt AI (WATCH, 5.25)
- Coldtea.ai (WATCH, 5.75)
- AgentSky (WATCH, 6.0)
- Smallest.ai (WATCH, 5.75)

61 other previously-logged tools were left untouched (not resurfaced by today's sources).

## Pruned from log

- **ai-emaily** — `last_seen` 2026-07-18, 31 days old, removed per the 30-day prune rule.

## Still pending your signup

None flagged today. `AFFILIATE_DB` currently lists 2 tools with `status: "pending"` (Semrush, Canva AI) — both were reminded within the last 7 days (Semrush 2026-08-12, 6 days ago; Canva AI 2026-08-16, 2 days ago), so neither crosses the 7-day re-reminder threshold yet.

---

**Summary: 2026-08-18 — 0 NEW, 0 UPGRADED, 6 REPEAT, 0 STALE_PENDING**
