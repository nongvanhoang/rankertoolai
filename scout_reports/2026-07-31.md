# Scout Report — 2026-07-31

## Reference-data note

`build_dashboard.py` is still not tracked on `main` (removed in commit `36fe6bf`, "Untrack internal automation scripts to fix live exposure on rankertoolai.com" — it was serving `AFFILIATE_DB` commission data publicly at `/dashboard/`). This run again recovered the last known `AFFILIATE_DB` snapshot from git history at `36fe6bf^:build_dashboard.py` (38 slugs, unchanged from prior runs) and cross-checked it against the live site's actual `/review/`, `/go/`, `/compare/`, `/alternatives/`, `/coupons/` directory listings on `main` (103 distinct base tool slugs — a more reliable "is this already covered" signal than the affiliate-mechanics dict, since it reflects published content directly) and `scout_log.json`'s 59 prior tool keys. Union: 162 known slugs going into today's scan. `html/build_dashboard.py` does not exist anywhere in this repo's history — confirmed again this run.

`semrush` still shows `status: "pending"` in the recovered `AFFILIATE_DB` snapshot, but this is stale data confirmed inactive-in-practice in prior runs (live tracking link already active at `main:go/semrush/index.html`). Kept out of `pending_reminders` again this run, consistent with prior runs.

## NEW

None today.

## UPGRADED

None today.

## Candidates researched and passed (not reported, for the record)

- **Keyword Grid (keywordgrid.com)** — AI-search-relevance analyzer (checks content the way LLM-based search/AI Overviews evaluate it); real credit-based pricing. On-niche (same category as Surfer SEO, Frase, Clearscope, MarketMuse — all already covered), but no confirmed affiliate program found after a direct search, and no strong distinct "why now" growth signal this week beyond general AEO-category noise (a crowded space already well-represented in `scout_log.json` via `profound`, `scrunch-ai`, `omniseo`, `openseo`, `rankscale`, `peec-ai`). Composite ~4.25. **PASS.**
- **Phantom (heyphantom.app)** — voice-first AI agent that operates macOS via the Menu Bar/notch, screen-context aware. Real Product Hunt listing, free starter tier + paid Pro/Max. However: searches for its affiliate program surfaced a *different* company also named "Phantom" (phantomleads.ai, an agency lead-gen tool with a confirmed recurring-commission affiliate program) — could not confirm heyphantom.app itself has any affiliate program, and no user/growth metrics were found beyond the PH listing itself. Kept out to avoid the same name-collision risk flagged for TrendTrack in a prior run. Composite ~4.5. **PASS** (revisit only if heyphantom.app's own affiliate program is independently confirmed).
- **Estera (tryestera.com)** — AI receptionist answering calls/WhatsApp 24/7 for SMBs; real Product Hunt presence and a plausible SMB affiliate niche (comparable in spirit to sales/support agents already tracked, e.g. `retell-ai`). Red flags: no public pricing page (tiers not disclosed), no confirmed affiliate program. Composite ~4.5. **PASS** (revisit once pricing is public and an affiliate program is confirmed).
- **Seedream (ByteDance image model)** — technically strong image generator, but distributed only through a fragmented set of third-party wrapper sites (dzine.ai, seedream-5.io, seedream4.org, seedreamimage.com, etc.) with no single canonical vendor or affiliate target — high risk of promoting the wrong/a low-quality clone site. **PASS** (not a single reviewable product under current distribution).
- **Nudge (nudge-26 on Product Hunt)** — AI weekly task auto-scheduler, solo-built by a student, $5-9/mo. Too small (no funding, no affiliate program, minimal growth signal beyond one PH launch). Composite ~3.0. **PASS.**

## REPEAT

Two previously-logged tools resurfaced in today's scan with fresh mentions but no material change to tier:

- **Velo AI** (`velo-ai-video`, WATCH) — "Velo 3.0" appeared in this week's Product Hunt AI leaderboard (new version release). No new funding/growth signal beyond the version bump. Tier unchanged: WATCH (composite ~5.25). `last_seen` updated to 2026-07-31.
- **Glaze by Raycast** (`glaze-raycast`, HOT) — resurfaced in this week's Product Hunt weekly ranking, consistent with its prior HOT rating from 2026-07-28. No new information changing the score. Tier unchanged: HOT (composite ~7.875). `last_seen` updated to 2026-07-31.

## Still pending your signup

None due this run. `canva-ai`, `lovable`, and `gamma` were last reminded 2026-07-26 (5 days ago) — the 7-day cooldown means they're not due again until 2026-08-02. `semrush` remains excluded from this list (confirmed active, see reference-data note above).

---

**Summary — 2026-07-31:** 0 NEW, 0 UPGRADED, 2 REPEAT, 0 STALE_PENDING.
