# Scout Report — 2026-08-09

## Data-source note

`build_dashboard.py` remains untracked on `main` (removed in commit `36fe6bf`, "Untrack internal automation scripts to fix live exposure on rankertoolai.com" — it was serving `AFFILIATE_DB` commission data publicly at `/dashboard/`). Consistent with every run since 2026-08-01, this run used the `already-covered` slug set built directly from the live site structure (`review/`, `compare/`, `alternatives/`, `go/` directory listings on `main` — 151 unique slugs) unioned with `scout_log.json`'s 67 tracked candidates, plus the stale `build_dashboard.py` snapshot still committed on the `scout-log` branch itself (last touched 2026-07-02) as the only available `AFFILIATE_DB`-shaped source for Step 5B.

`semrush` and `canva-ai` are the only two entries the stale snapshot lists as `status: "pending"`. `semrush` was independently re-verified this run (`main:go/semrush/index.html` still redirects through a live Impact.com tracking link, `semrush.sjv.io/...`) — genuinely resolved, not pending, consistent with the 2026-08-08 housekeeping fix; left out of `pending_reminders` entirely. `lovable` and `gamma` still have no entry in any `AFFILIATE_DB` source available to this run and no live page on `main` — left untouched rather than guessed at (same as every run since 2026-08-01).

## NEW

None today.

## UPGRADED

None today.

## Considered and rejected (PASS — not tracked further)

- **Port22** ("Claude Code, Codex & more on your phone," Product Hunt launch this week) — puts coding agents running on your Mac onto your phone with push-to-approve permissions. Real product, decent PH buzz, but free for one Mac/two sessions with every feature on and no discoverable paid tier or pricing page found. No monetization path to affiliate. Red flag: no real pricing page.
- **LumiChats** (pay-per-use multi-model AI aggregator for students/devs — Claude, GPT-5, Gemini access from ~$7-14/mo) — real recurring pricing, but no affiliate program found (searched directly), and the multi-model-aggregator space is already crowded (Poe and similar). Composite ~4.75 (growth 6, market 6, affiliate 3, competition_gap 4) — below WATCH threshold.
- **claudemon** (Pokémon-in-your-terminal plugin for Claude Code, PH launch this week) — fun, viral-ish, but explicitly free/open-source under MIT with no monetization whatsoever. Red flag: no real pricing page.
- **Twitter Personality by Wordware** (viral AI agent, 4M users in 12 days analyzing X accounts) — huge growth signal, but it's a single free feature folded into the Wordware platform with no standalone pricing or affiliate program of its own. Red flag: no real pricing page for this specific product.
- **Simile** ($200M Series B at $2B valuation for AI "synthetic consumer twin" market research, closed 2026-07-30) — massive funding signal, but enterprise sales-led only (CVS, Deloitte, Wealthfront as clients), no self-serve pricing page. Red flag: no real pricing page / enterprise-only.
- **Digen AI** (AI video generation agent, multi-model access to Sora 2/Veo 3.1/Kling 3.0) — real pricing (up to $199.99/mo) and has an affiliate program, but it's already extensively reviewed across many existing sites (There's An AI For That, Filmora, AgentsPointee, and others) and isn't a fresh find — the "before competitors cover it" window is already closed. Composite ~4.75 (growth 5, market 7, affiliate 5, competition_gap 2) — below WATCH threshold on competition_gap.

## REPEAT (suppressed, log updated only)

6 tools re-observed today, all resurfacing in Product Hunt / social coverage with no material change to prior scoring — `last_seen` and `tier_history` updated only:

- **Zinley** (WATCH 5.5) — topped this week's Product Hunt leaderboard by vote count.
- **AgentSky** (WATCH 6.0) — #2 on this week's Product Hunt leaderboard.
- **Ctruh Studio** (WATCH 5.5) — top-5 on this week's Product Hunt leaderboard.
- **NudgeForMe** (WATCH 6.25) — top-5 on this week's Product Hunt leaderboard.
- **Airtop** (WATCH 7.25) — "Airtop for Google Ads Automation" featured on this week's Product Hunt leaderboard.
- **Tweet Hunter** (WATCH 7.0) — surfaced again in X/Twitter-growth-tool coverage.

## Pruned from log (>30 days inactive)

None this run. Four entries (`circlechat`, `granola`, `meev`, `thesys`) sit at exactly 30 days since `last_seen` (2026-07-10) — not yet over the threshold, will prune next run if they don't resurface.

## Still pending your signup

- **canva-ai** — first flagged 2026-07-19 (see that report for original affiliate context), 21 days ago. Re-verified today: `main:go/canva-ai/index.html` still points to plain `canva.com` with no tracking parameter — genuinely still pending. Last reminder was 2026-08-02 (7 days ago, due again today). `pending_reminders.canva-ai.last_reminded` updated to 2026-08-09.

`semrush` remains resolved (see data-source note above) and stays out of tracking. `lovable` / `gamma` remain untouched pending a real data-source fix (no entry in any available `AFFILIATE_DB` source, no live page on `main`).

---

**Summary:** 2026-08-09 — 0 NEW, 0 UPGRADED, 6 REPEAT (suppressed), 1 STALE_PENDING (canva-ai).
