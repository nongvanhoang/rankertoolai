# Scout Report — 2026-09-19

## ⚠️ Pipeline health notice (read first)

This scheduled run hit two structural problems that affect how much to trust today's output. Flagging both before the findings so they don't get lost:

1. **Step 1's data source no longer exists.** The task spec says to read `build_dashboard.py` (root and `html/`) on `main` for the `AFFILIATE_DB` dict. Neither file exists on current `main` (`146611b`) — the site was restructured at some point after this branch's last run (2026-08-20) and no longer uses that pattern. I substituted the live `review/`, `compare/`, `alternatives/`, and `go/` directory listings on `main` (286 entries) as the "already covered" set instead — functionally equivalent, arguably more accurate — but Step 5B's "AFFILIATE_DB entries with `status == pending`" has **no data source at all** anymore, so I could not run the stale-pending-signup check this run. STALE_PENDING is reported as **unknown**, not empty — do not read silence there as "nothing pending."
2. **A second, newer scout system has superseded this one.** `main` now carries `.claude/agents/rankertoolai-scout.md` + `scout_seen.json`, a differently-shaped tracker that has been updated daily through **today** (last entries `2026-09-19`: Wisry, Fraime). This `scout-log` branch pipeline went dormant for exactly 30 days (last commit 2026-08-20) — almost certainly *because* of problem #1 breaking it silently, right around the time the newer system took over. The two systems now have non-overlapping candidate pools and no reconciliation between them (e.g. `AIR`, `Monid` are tracked by `scout_seen.json` since early September but are invisible to this branch's log, and vice versa). Recommend the user decide which system should be the source of truth and retire the other — running both risks duplicate/contradictory tool recommendations going forward.

**Because of #2, I skipped the mechanical "prune entries with last_seen >30 days old" step this run.** Applying it literally would have deleted tracking history for 58 of this log's 66 tools — including **Lovable** and **Gamma**, the exact tools this task's own Step 5B instructions name as still-pending-signup examples — purely because the pipeline was dormant for the gap, not because those tools actually went stale. Wiping that history looked like clear unintended data loss, so I left `scout_log.json`'s tool history untouched except for one legitimate update (see below), and I'm flagging the prune decision for the user to confirm rather than making it silently.

## Today's pulse scan

Exa AI search was unavailable this run (`EXA_API_KEY` empty in this environment, and direct network access to `api.exa.ai` is blocked by the sandbox proxy — confirmed via a raw `curl`, not just an empty result). Per the fallback rule, all five topic queries ran through WebSearch instead. WebSearch's general web index does not have good visibility into genuine last-24-48h launch signal (Product Hunt daily leaderboard, funding wires, live Reddit/X threads) — it mostly surfaced month-level roundup/aggregator pages rather than dated, verifiable events. Signal quality this run should be considered **degraded**, not equivalent to a normal Exa-backed scan.

What did surface, evaluated and rejected:

| Candidate | Why not reported as NEW |
|---|---|
| GPT-6 Astra (OpenAI) | Not a new company/product — OpenAI's own model; ChatGPT already covered on-site |
| Rhoda AI ($450M Series A) | Robotics/video-predictive-control hardware play, not a self-serve SaaS tool — poor affiliate fit |
| Nina (Anthalpha) | AI agent for crypto trading — regulatory/ToS risk (RED FLAG territory), skipped |
| AIR ($50M seed, AI agent security) | Real Sep-1 signal, but already tracked by `scout_seen.json` since 2026-09-02 (not fresh to this pulse) and not in this branch's log — duplicate across systems, not a same-run finding |
| Agent Builder (Airtop) | Airtop is already tracked in this log (WATCH, 7.25, first seen 2026-08-06) — feature announcement only, logged as a REPEAT sighting (see below), not new |
| T-Rex Label (vision dataset annotation) | Narrow enterprise/ML-tooling niche, self-serve pricing unclear — insufficient signal to score |
| Atira ($17.5M, industrial dealmaking AI) | Enterprise/industrial B2B, likely sales-led not self-serve — insufficient signal to score |
| Switch, Mastra Factory, Monid, AI Toolbox 3.0, Loqua, Harden, Tadata, Articos, dif.sh | Names only, from an undated aggregator leaderboard — no real info on pricing/affiliate/traction to score responsibly. (Monid is separately already tracked by `scout_seen.json` since 2026-09-04.) |

No candidate cleared the bar for a real, defensible HOT/WATCH score this run.

## Log update

- `airtop`: REPEAT sighting (Agent Builder feature launch) — `last_seen` bumped to 2026-09-19, tier_history appended (WATCH, 7.25, unchanged).
- Pruning of >30-day-stale entries **skipped this run** — see pipeline health notice above.

## Still pending your signup

**Unknown / could not check.** Step 5B requires reading `status == "pending"` entries from the `AFFILIATE_DB` dict in `build_dashboard.py`, which no longer exists on `main` (see notice above). This is not the same as "nothing pending" — the task prompt's own examples (Lovable, Gamma) suggest there may still be unfinished signups. Recommend checking the newer `scout_seen.json`/agent-based system, or restoring a queryable affiliate-status source, to get a real answer here.

---

**Summary: 2026-09-19 — 0 NEW, 0 UPGRADED, 0 REPEAT (reported; 1 REPEAT logged silently per spec), STALE_PENDING unknown (data source missing, see notice above).**
